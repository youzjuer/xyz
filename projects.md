# Projects index

## Active projects

- `proj1` — 炒股目标项目示例

## Template

新增项目时：

1. 在 `projects/<project-id>/` 下创建目录。
2. 新建 `PROJECT.md`。
3. 在 `tasks/` 下创建该项目的任务文件。
4. 为每个任务指定独立的 `runs/<project>/<task>/` 输出目录。
