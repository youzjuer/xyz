# AGENTS

这是本项目的工作手册。Agent 在这个目录下工作时优先遵守这里的规则。

## 项目目标

维护一套可复制的 Hermes 配置工程，让本地 Agent 具备人格、记忆、技能、定时任务和通知渠道的基础骨架。

## 文件约定

- `SOUL.md`：Agent 人格与行为边界。
- `USER.md`：用户长期画像，不写临时上下文。
- `MEMORY.md`：长期记忆索引。
- `skills/`：可复用工作流，每个 Skill 一个 Markdown 文件。
- `cron/`：定时任务示例和运行约定。
- `gateway/`：通知渠道与 Home Channel 模板。
- `migration/`：迁移清单。
- `rag/`：知识库/RAG 接入说明。
- `logs/`：定时任务与流水线日志。
- `runs/`：一次性任务产物。

## 工作规则

- 先复用现有模板，再新增文件。
- Skill 必须写清楚目标、输入、输出、流程、失败处理。
- 定时任务要说明频率、触发条件、输出位置、安静模式规则。
- 任何密钥都放环境变量或本机密钥管理器，不写进仓库。
- 对外发布、删除文件、覆盖记忆、修改权限前先确认。

## 提交前检查

- 没有 `.env`、token、cookie、私钥、真实账号凭据。
- 模板能脱离当前会话单独阅读。
- 运行产物进入 `logs/` 或 `runs/`，不要混在配置目录。
- 新增记忆必须是长期有用信息，不是任务流水账。
